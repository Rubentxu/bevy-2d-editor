[workspace]
members = ["crates/editor-core", "crates/ai-proxy", "crates/editor-model", "crates/editor-application"]
resolver = "2"

# Profile configuration is inherited from ~/.cargo/config.toml
# to share settings across all Rust projects and avoid redundant definitions.
