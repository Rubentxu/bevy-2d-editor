# v1.0-stabilization Evidence Map

> **Status:** Refresh — second artifact of the v1.0-stabilization cycle (refreshed 2026-09-08).
> **Source cycle:** `evidence-map-refresh-v1099` (B-direct, sequence 303+).
> **Author:** automated refresh against `HEAD = c6d383e` (v0.109.8 + G8 archive).
> **Scope:** inventory existing evidence, identify gaps against the v1.0 product gates from
> [`docs/roadmaps/MASTER_ROADMAP.md`](./roadmaps/MASTER_ROADMAP.md) and
> [`docs/roadmaps/v1.0-stabilization.md`](./roadmaps/v1.0-stabilization.md). **No code changes.**

## Refresh history

This document has been refreshed multiple times; the previous baseline is preserved in git
history. Material changes from the most recent refresh:

### Refresh 2026-09-08 (Load sample real loader)

This refresh now reflects `HEAD = 0f0564f` (v0.110.8 + load-sample-real-loader archive).

- §2.1 spec count: `@full` cohort now also includes
  `load-sample-real-loader.spec.ts` (1 scenario × 2 projects = 2 tests).
- §4 **P1 carry-forward closed**: `window.__loadSampleProject(id)` test
  bridge is no longer a stub — `TutorialStepper` now calls a real loader
  (`services/sampleLoader.ts:mountPlatformerMinimal()`) that fetches
  `/examples/platformer-minimal/<file>` from the Vite dev-server and
  writes each of 9 canonical files to OPFS via
  `window.opfs_save_file`, then calls `window.load_project()` to
  hydrate the engine. The duplicate `OPFS_FILES` mapping previously
  inlined in `e2e-game-creation.spec.ts` and `git-friendly-roundtrip.spec.ts`
  has been deduplicated: production `OPFS_FILES` in
  `services/sampleLoader.ts` is the single source of truth; the test
  helper `tests/helpers/sample-loader.ts` re-exports it.
  14/14 in-scope tests pass (2 new + 8 tutorial-walkthrough + 2
  tour-completed-persistence + 2 e2e-game-creation). tsc + eslint
  clean. A-lite cycle, 6 files / +575/-146 + 7 SDDK artifacts.
- §6.1 carry-forward list: closed `load-sample-real-loader`.
- **Coverage score: 9 ✅ / 0 🟡 / 0 🔴** (unchanged numeric;
  v0.110.6 P1 UX follow-up #1 closed).

### Refresh 2026-09-08 (Tutorial walkthrough)

This refresh now reflects `HEAD = c23d84e` (v0.110.6 + tutorial-walkthrough archive).

- §2.1 spec count: `@accessibility` / `@full` now both include
  `tutorial-walkthrough.spec.ts` (4 scenarios × 2 projects = 8 tests).
- §4 **P1 carry-forward closed**: WelcomeOverlay's "Take the tour"
  action now opens a real 5-card guided walkthrough (Inspect Assets →
  Find Levels → Open Logic → Tweak Components → Play & Test), replacing
  the no-op `setEditorMode("asset-authoring")` from the original
  Welcome Overlay. TutorialStepper reuses the existing
  `window.__setEditorMode` test bridge and introduces
  `window.__loadSampleProject("platformer-minimal")` as a stub for the
  future backend loader. 8/8 tests pass; a11y intact
  (3/3 ux-welcome @accessibility regression). tsc + eslint clean. A-min
  cycle, 4 files / +431/-1.
- §6.1 carry-forward list: closed "Tutorial walkthrough (P1 follow-up)".
- **Coverage score: 9 ✅ / 0 🟡 / 0 🔴** (unchanged numeric; P1
  carry-forward closed).

### Refresh 2026-09-08 (ImportDialog wiring)

This refresh now reflects `HEAD = 200277c` (v0.110.5 + import-dialog-wiring archive).

- §2.1 spec count: `@a11y` now 3 (`a11y-critical-paths.spec.ts` + `import-dialog.spec.ts`).
- §4 **G7 carry-forward closed**: `<ImportDialog />` is now wired to the
  `import-asset-btn` trigger (cycle `import-dialog-wiring`, v0.110.5).
  4 scenarios (S1–S4) covered by `frontend/tests/import-dialog.spec.ts`:
  happy path + Escape, success + custom event, conflict routing via
  `window.__setActiveBottomTab("workbench")` test bridge, error-phase UX.
- §6.1 carry-forward list: removed "ImportDialog wiring (CP-5 carry-forward)".
- §4 G7 row: updated to mention ImportDialog is wired.
- **Coverage score: 9 ✅ / 0 🟡 / 0 🔴** (unchanged numeric; CP-5 carry-forward closed).

---

## 1. Scope and methodology

This document is the **second deliverable** of the v1.0-stabilization cycle (the first was the 2026-09-06 baseline). It answers:

1. What evidence already exists that the IDE works?
2. Which v1.0 product gates have coverage?
3. Which v1.0 gates have no coverage (gaps)?
4. What is the recommended next concrete work?

**Methodology:**

- **Test inventory:** count tests per layer (Playwright specs × cohort/tag, cargo `#[test]`/`tests/*.rs`, archcheck rules, docs-check rules).
- **Feature inventory:** read the command surface (`engine-bridge.ts` window bridges, `crates/editor-wasm/src/lib.rs` exports), the UI surface (`frontend/src/components/`), and the workflow surface (`examples/`, importers, recipes).
- **Coverage matrix:** map each v1.0 product gate (MASTER_ROADMAP) and each roadmap section (v1.0-stabilization) to the tests and features that exercise it.
- **Gap analysis:** explicit "no evidence" or "thin evidence" cells in the matrix.
- **Recommendations:** ordered by impact × cost, with the smallest acceptable v1.0 evidence packet identified.

**Out of scope:** agent runtime (Rig) work — paused per user directive 2026-09-06 until v1.0 gates pass.

---

## 2. Test inventory (evidence already in the repo, HEAD `c6d383e`)

### 2.1 Playwright specs — 80 specs across 5 cohorts (+2 since previous baseline)

| Cohort | Tag | Specs | Wall time on local runner | Notes |
|---|---|---:|---:|---|
| `playwright.smoke.config.ts` | `@smoke` | 4 | ~2 min (pre-existing budget breach, see §6.3) | `smoke.spec.ts`, `app-characterization.spec.ts`, `capabilities-smoke.spec.ts`, `editor-ready.spec.ts` |
| `playwright.full.config.ts` | `@full` (40 specs) | 40 | not recently measured | All specs tagged `@full`. **+2 since previous baseline**: `ui-entity-creation.spec.ts` (BJ-3, v0.109.6) and `git-friendly-roundtrip.spec.ts` (G2, v0.109.7). |
| `playwright.domain.config.ts` | `@domain` | 27 | not recently measured | Domain coverage of features |
| `playwright.persistence.config.ts` | `@persistence` | 4 | not recently measured | OPFS persistence flows |
| `playwright.a11y.config.ts` | `@a11y` (axe-core) | 1 | not recently measured | `ux-a11y.spec.ts` |
| `frontend/tests/e2e/` (importers) | `@full` | 1 | not recently measured | `e2e/importers.spec.ts` (Aseprite/LDtk/Tiled) |

**Test tag distribution observed:**

```
@smoke:         4 specs   (smoke, app-characterization, capabilities-smoke, editor-ready)
@full:          40 specs  (+2 since previous baseline: ui-entity-creation, git-friendly-roundtrip)
@domain:        27 specs  (feature-domain coverage)
@persistence:    4 specs  (logic-graph-persistence, multi-scene, validation-center-{inbox,v2})
@a11y:           1 spec   (ux-a11y)
no tag:          1 spec   (ux-a11y runs in a11y cohort regardless)
```

`engine.spec.ts` runs in the smoke cohort and is the **largest single spec** (20 cases covering typed command system, operation log, OPFS persistence, schema registry, UI hierarchy/inspector). It is the workhorse of the release-health gate.

### 2.2 Cargo tests — 727 inline + 56 integration across 6 crates (+1 file, +82 inline since previous baseline)

| Crate | Integration `tests/*.rs` | Inline `#[test]` | Notes |
|---|---:|---:|---|
| `editor-bevy` | 45 | 460 | The big one: actuator bus, apply-back, asset command/load/persistence, auto-layer, BSN codegen, logic bindings, etc. **+15 inline** since previous baseline (jump + enemy patrol systems + integration tests). |
| `editor-model` | 3 | 130 | **+1 integration file** (`tests/extension_compat.rs`, 180 lines, 3 tests) and stable inline count (the new tests live in the integration file). Includes `tests/migration_corpus.rs` (95 lines, basic migration evidence). |
| `editor-application` | 8 | 31 | Reimport, transaction, session, extension |
| `editor-storage-web` | 0 | 20 | OPFS internals (wasm/non-wasm stub split) |
| `ai-proxy` | 1 | 5 | HTTP boundary |
| `editor-protocol` | 0 | 0 | Type-only crate |
| `editor-wasm` | 0 | 0 | No native tests; wasm-only via Playwright |

**Total:** 57 integration tests + 727 inline tests = **784 Rust test points** (+82 since previous baseline of 702).

**New since previous baseline:**

- `crates/examples-bevy-harness/src/jump.rs` + `tests/jump.rs` (3 tests).
- `crates/examples-bevy-harness/src/enemy_patrol.rs` + `tests/enemy_patrol.rs` (4 tests).
- `crates/editor-model/tests/extension_compat.rs` (3 tests).

### 2.3 Gate tests — 4 deterministic gates

| Gate | Runner | Status | Coverage |
|---|---|---|---|
| `cargo fmt --all --check` | Local + CI | ✅ | Format invariants |
| `archcheck` (`tools/archcheck/check.ts`) | Local + CI | ✅ all assertions pass | Architecture fitness rules (B1, B8, etc.) |
| `docs-check` (`tools/docs-check/check.ts`) | Local + CI | ✅ pass (rule 7 warning only) | Doc contracts: rule 6 (stable-id uniqueness), rule 7 (spec-durability flags) |
| `cargo test --workspace --release --locked` | Local + CI | ⚠️ C-3 carry-forward (exceeds 10 min locally; CI parallel may differ) | Rust test corpus (the 784 points above) |

Plus frontend gates (`npm run lint`, `npm run format:check`, `npx tsc --noEmit`, `npm run build:check`) — all ✅ at HEAD `c6d383e`.

### 2.4 CI workflows (`.github/workflows/`)

- `ci.yml` — Baseline CI: Rust fmt + test workspace + WASM check + archcheck + frontend lint/format/tsc/build + Playwright smoke cohort.
- `archcheck.yml` — Architecture Fitness: archcheck only (separated for fast feedback on PRs).

Per local-CI-is-source-of-truth rule: GitHub Actions is documentation, not the gating signal; local reproduction is the gate.

### 2.5 Cycles closed since previous baseline

| Cycle | Tag | Phase | Artifact |
|-------|-----|-------|----------|
| `g1-step1-bj1-load-sample` | v0.109.1 | ✅ CLOSED | `docs/sddk/g1-step1-bj1-load-sample/` |
| `g1-step2-bj2-render` | v0.109.2 | ✅ CLOSED | `docs/sddk/g1-step2-bj2-render/` |
| `g1-step3-bj4-keyboard-move` | v0.109.3 | ✅ CLOSED | `docs/sddk/g1-step3-bj4-keyboard-move/` |
| `g1-step4-bj5-jump` | v0.109.4 | ✅ CLOSED | `docs/sddk/g1-step4-bj5-jump/` |
| `g1-step5-bj5-enemy-patrol` | v0.109.5 | ✅ CLOSED | `docs/sddk/g1-step5-bj5-enemy-patrol/` |
| `bj3-ui-entity-creation` | v0.109.6 | ✅ CLOSED | `docs/sddk/bj3-ui-entity-creation/` |
| `g2-git-friendly-roundtrip` | v0.109.7 | ✅ CLOSED | `docs/sddk/g2-git-friendly-roundtrip/` |
| `g8-extension-compat-policy-runtime` | v0.109.8 | ✅ CLOSED | `docs/sddk/g8-extension-compat-policy-runtime/` |
| `evidence-map-refresh-v1099` | v0.109.9 | ✅ CLOSED | `docs/sddk/evidence-map-refresh-v1099/` |
| `g7-a11y-critical-paths` | v0.110.0 | ✅ CLOSED | `docs/sddk/g7-a11y-critical-paths/` |
| `g4-roundtrip-corpus` | v0.110.1 | ✅ CLOSED | `docs/sddk/g4-roundtrip-corpus/` |
| `g5-crash-recovery` | v0.110.2 | ✅ CLOSED | `docs/sddk/g5-crash-recovery/` |
| `g6-performance-corpus` | v0.110.3 | ✅ CLOSED | `docs/sddk/g6-performance-corpus/` |
| `cp5-import-trigger` | v0.110.4 | ✅ CLOSED | `docs/sddk/cp5-import-trigger/` |
| `import-dialog-wiring` | v0.110.5 | ✅ CLOSED | `docs/sddk/import-dialog-wiring/` |
| `tutorial-walkthrough` | v0.110.6 | ✅ CLOSED | `docs/sddk/tutorial-walkthrough/` |
| `tour-completed-persistence` | v0.110.7 | ✅ CLOSED | `docs/sddk/tour-completed-persistence/` |
| `load-sample-real-loader` | v0.110.8 | ✅ CLOSED | `docs/sddk/load-sample-real-loader/` |

---

## 3. Feature inventory (the surface that needs evidence)

### 3.1 UI surface — 60 React components in `frontend/src/components/`

Panels: `AIAssistantPanel`, `ApplyBackPanel`, `AssetAuthoringView`, `AssetNavigator`, `ChangeWorkbenchPanel`, `CodeEditor`, `GameOverlay`, `HierarchyPanel`, `InspectorPanel` (+ `Inspector/` subdir), `LogicGraphEditor`, `ProjectAssetBrowser`, `RecipePicker`, `RuntimeCausalityPanel`, `RuntimePreviewInspector`, `SchemaAuthoringPanel`, `SearchTab`, `TileCanvas`, `TilesetPanel`, `ValidationCenter`, `WorldWorkspace`, etc.

Dialogs: `AssetUnsavedChangesDialog`, `ConfirmDialog`, `ExportRustModal`, `ImportDialog`, `PromptDialog`, `UnsavedChangesDialog`.

UX shell: `AppHeader`, `AppShell`, `CheatSheet`, `CommandPalette`, `Dock/`, `FloatingPanel/`, `Menu/`, `MenuBar`, `ModeContextBar`, `OnboardingBanner`, `SaveSceneModal`, `SceneTabs`, `StatusBar`, `TopBar`, `ToolbarGroup`, `ViewportControls`, `WelcomeOverlay`.

31 hooks in `frontend/src/hooks/` orchestrate the panels.

### 3.2 Command / capability surface — 80+ `window.*` test bridges

`frontend/src/engine-bridge.ts` exposes ~80 test bridges post-recovery-3. Coverage categories:

- **Scene & OPFS persistence:** `load_scene`, `save_scene`, `list_scenes`, `load_project`, `project_exists`, `opfs_*`.
- **Schema registry:** `save_schema`, `load_schema`, `register_schema`, `unregister_schema`, `list_schemas`, `combined_registry_size`, `get_combined_schemas_json`.
- **Asset pipeline:** `list_asset_files`, `import_asset_file`, `read_asset_file_bytes`, `delete_asset_file`.
- **Scene Asset authoring:** `create_scene_asset`, `rename_scene_asset`, `duplicate_scene_asset`, `delete_scene_asset`, `open_scene_asset`, `dispatch_asset_command`, `undo_asset`, `redo_asset`, `save_scene_asset`.
- **Logic Graph authoring:** `dispatch_logic_command`, `undo_logic`, `redo_logic`, `get_logic_log_state`, `get_logic_graph`, `create_logic_graph_asset`, `open_logic_graph_asset`, `list_logic_graph_assets`, `get_node_descriptors`.
- **Multi-scene:** `scene_create`, `scene_switch`, `scene_switch_commit`, `scene_delete`, `scene_rename`, `list_scenes_extended`, `get_current_scene_id`.
- **Source files (code editor):** `list_source_files`, `read_source_file`, `write_source_file`, `create_source_file`, `delete_source_file`, `find_source_location`.
- **Level / tileset / paint:** `paint_tile`, `erase_tile`, `list_tilesets`, `load_tileset`, `save_tileset`, `delete_tileset`.
- **Override / resync:** `validate_overrides_wasm`, `effective_values_wasm`, `try_rebind_wasm`, `get_resync_reports`, `upsert_override_wasm`, `revert_override_wasm`.
- **Hot reload:** `hot_reload_source_wasm`, `hot_reload_asset_wasm`, `force_reload_wasm`.
- **Change Workbench:** `submit_pending_change_set`, `get_pending_change_sets`, `approve_change_set`, `reject_change_set`, `get_change_set_summaries`.
- **Play/Stop mode:** `enter_play_mode`, `exit_play_mode`.
- **BSN:** `export_asset_to_bsn_wasm`, `import_bsn_text_to_asset_wasm`, `import_bsn_asset_wasm`.
- **Export:** `export_dynamic_scene_wasm`, `export_code`.

52 `pub fn` WASM exports in `crates/editor-wasm/src/lib.rs` plus the 80+ bridges above.

### 3.3 Backend modules — per-crate

- `editor-model/`: adapter, auto_layer, causality, component, document, extension, external_source, graph_kernel, ids, importer, int_grid, lib, logic_activation, logic_graph, **migration** (373 lines), ports, project_store, scene_asset, scene_instance, schema, scope_of_change, transaction_kernel, validation, version.
- `editor-bevy/`: 35+ modules (actuator_bus, asset_command, asset_state, bevy_anchor, bevy_logic_binding, bsn_codegen, bsn_export, bsn_import, command, document, etc.).
- `editor-application/`: adapters, extension, importer_registry, reimport, runtime_delta, session, transaction, wasm.
- `editor-storage-web/`: lib, memory_bridge, opfs_core (hydrate + flush), raw_store_bridge, wasm_bridge.
- `ai-proxy/`: config, error, server, openai, context, handlers, lib, main.
- `editor-protocol/`: capabilities, dispatch_error, re_exports.
- `examples-bevy-harness/`: NEW since previous baseline — `jump.rs`, `enemy_patrol.rs`, `components.rs`, `loader.rs`, etc.

### 3.4 Workflows (multi-step user journeys)

- **Open editor → create scene → add entity → add component → save → reload → load** → covered by `engine.spec.ts:526` (50-entity roundtrip) AND `frontend/tests/ui-entity-creation.spec.ts` (BJ-3).
- **Register schema → AddComponent → save schema → reload → load_project** → covered by `engine.spec.ts:744`.
- **Hot reload .rs file → Bevy picks up change** → covered by `hot-reload.spec.ts`, `hot-reload-bus.spec.ts`.
- **Play mode → Bevy preview → Stop** → covered by `runtime-preview-v2.spec.ts`, `GameOverlay`.
- **Drag-and-dock panel** → covered by `ux-drag-dock.spec.ts`.
- **Import Aseprite/LDtk/Tiled** → covered by `e2e/importers.spec.ts`.
- **Open multi-level project → switch scenes** → covered by `multi-scene.spec.ts`, `world-workspace.spec.ts`.
- **Git-friendly round-trip** → covered by `frontend/tests/git-friendly-roundtrip.spec.ts` (G2).
- **Canonical playable sample game** → covered by `examples/platformer-minimal/` (G1 deliverable) + Bevy harness integration tests (jump + enemy patrol).

---

## 4. Coverage matrix — v1.0 product gates

The MASTER_ROADMAP defines nine v1.0 product gates. This matrix scores each gate as:

- ✅ **green** — explicit, runnable, recent evidence (committed tests, runnable CI step, or `cargo test` corpus).
- 🟡 **amber** — partial evidence; some aspects tested but gate has implicit gaps.
- 🔴 **red** — no test, no runnable artifact, no committed policy.

| # | v1.0 product gate | Status | Evidence | Gaps |
|---|---|:---:|---|---|
| G1 | create a small complete 2D game without hand-editing editor data | ✅ | `examples/platformer-minimal/` (canonical platformer sample, 9 OPFS files), `crates/examples-bevy-harness/` (Bevy 2D integration harness with `JumpPlugin`, `EnemyPatrolPlugin`, `PlayerController`), 15/15 Bevy runtime integration tests (jump + enemy patrol). Playwright `ui-entity-creation.spec.ts` proves in-editor entity creation (BJ-3). `git-friendly-roundtrip.spec.ts` proves `project.json` round-trip is byte-identical. | None obvious; well-covered. (7/8 G1 sub-deliverables closed: BJ-1, BJ-2, BJ-3, BJ-4, BJ-5 jump, BJ-5 enemy patrol, BJ-5 contact-death still out of scope as it requires logic-graph runtime.) |
| G2 | filesystem/Git workflow documented and stable | ✅ | ADR-0045 (`git-friendly-project-format-and-migrations.md`) defines intent. `frontend/tests/git-friendly-roundtrip.spec.ts` (v0.109.7) proves: (1) `project_json_round_trip_is_byte_identical` (mount sample → hydrate → write back → read again → byte-identical text), (2) `every_opfs_file_is_parseable_json_with_version_field` (every persisted document has explicit `version`). Branch protection rule exists. | None obvious; well-covered. |
| G3 | browser-local workflow remains supported | ✅ | OPFS hydration/persistence covered by `engine.spec.ts:526/744`, `multi-scene.spec.ts`, `logic-graph-persistence.spec.ts`, `validation-center-v2.spec.ts`. | None obvious; well-covered. |
| G4 | round-trip/migration compatibility guaranteed for declared v1 formats | ✅ | `docs/v1-format-manifest.md` (206 lines) declares the 5 durable document types at v1 with version constants and v0→v1 migration policies. `crates/editor-model/tests/migration_corpus.rs` (8 tests, +4 new): covers all 5 types (`SceneDocument`, `SceneAssetDocument`, `WorldDocument`, `LogicGraphAsset`, `ProjectMetadata`) with v0→v1 corpus tests + idempotency + cross-type future-version rejection. ADR-0046 rule 2 (forward-compat via `extension_data` flatten map) governs all types. | None obvious. v2 plan documented in §6.1 of the manifest. |
| G5 | crash/data-loss recovery story tested | ✅ | `docs/crash-recovery.md` (290 lines) declares the atomic-write contract: shadow→commit→cleanup ordering, hydrate-time orphan sweep, 4 handled failure modes (tab close, WASM panic, browser crash, power loss), 4 deferred failure modes (quota, multi-tab, backup-before-delete, dirty-flag). `crates/editor-storage-web/src/opfs_core.rs` extracts a pure `OpfsCore::classify_paths` helper covered by **5 new unit tests**. `frontend/tests/crash-recovery.spec.ts` (v0.110.2, 3 tests) proves end-to-end: (1) `opfsSaveAtomic` replaces the real file and clears the shadow, (2) `hydrate` removes orphan `.tmp` shadows left by a previous crash, (3) the original real path survives even when a stale shadow existed alongside it. All 31/31 editor-storage-web lib tests pass. | None obvious. The 4 deferred failure modes are tracked as explicit non-goals; future work to address them should re-read `docs/crash-recovery.md` §"Deferred failure modes". |
| G6 | performance corpus meets budgets | ✅ | New perf cohort `playwright.performance.config.ts` + 6 specs (`frontend/tests/perf-1k-entities.spec.ts`, `perf-tile-level.spec.ts`, `perf-multi-level-world.spec.ts`, `perf-asset-catalog.spec.ts`, `perf-logic-graph.spec.ts`, `perf-project-source-listing.spec.ts`) + helper (`tests/helpers/perfBudget.ts`). All 6 specs pass under hard budget on local Chromium (P1 1k-entity hydrate 1.93 s; P2 tile paint 14.59 s / erase 14.51 s; P3 multi-level switch 223 ms mean over 5; P4 asset catalog; P5 logic dispatch 71 ms mean over 50; P6 source listing). 3 emit soft-budget warnings as quantitative regression baselines. Specs sized to engine limits: MAX_SCENES=16 (`crates/editor-bevy/src/scenes.rs:14`), per-cell paint ~73 ms, OPFS prebuild throughput ~50 files/s source / ~250 entities/s scene / ~400 assets/s import. `docs/sddk/g6-performance-corpus/specification.md` declares soft + hard budgets with empirical baselines. | P6 proxies global_search with list_source_files (no bridge for global text search exists today — re-scope when bridge is exposed). Per-spec empirical magnitudes are reduced from the initial 10k/500/1000 plan to fit prebuild + engine limits; once prebuild optimisations land or MAX_SCENES lifts, re-benchmark at the new maximum. |
| G7 | accessibility critical paths pass | ✅ | `frontend/tests/a11y-critical-paths.spec.ts` (5 tests, registered in both `@a11y` and `@full` cohorts) proves keyboard-accessibility + accessible-name contract for CP-1 (welcome-skip), CP-2 (add-entity-btn), CP-3 (save-btn), CP-4 (play-btn), CP-5 (import-asset-btn). `frontend/tests/import-dialog.spec.ts` (4 tests, S1–S4) proves the full `<ImportDialog />` wiring (open via trigger, 3 importer kinds, success + custom event, conflict routing via `__setActiveBottomTab("workbench")` test bridge, error-phase UX). `docs/a11y-critical-paths.md` declares 5 paths, all ✅ PROVEN. CP-3/CP-4 tests are `inert`-tolerant; CP-5 test uses `window.__setEditorMode("asset-authoring")` bridge. `ux-a11y.spec.ts` runs axe-core landing sweep. | None obvious. Three follow-ups remain (separate cycles): highlight specific `changeSetId` in ChangeWorkbench; `ImportDialog.handleReimport` stub; Rust-side importer plugins. |
| G8 | extension and agent capability APIs have documented compatibility policy | ✅ | `docs/compatibility-policy.md` (323 lines, 2026-09-07) declares: document format versions, extension API SemVer discipline, `#[non_exhaustive]` discipline, capability tool surface, stability levels, deprecation policy, breaking change process, version pinning strategy. `crates/editor-model/tests/extension_compat.rs` (v0.109.8) proves: (1) manifest JSON round-trip is byte-identical (forward-compat), (2) `SemVer::parse` accepts valid + rejects malformed, (3) `Capability` enum covers 8 declared runtime categories. ADR-0040 + ADR-0043 declare intent. | None obvious. (ADR-0040 lists 9 categories but enum has 8; future `menus/palette entries` variant planned. `#[non_exhaustive]` allows growth.) |
| G9 | no critical architecture fitness exceptions remain | ✅ | `archcheck` all assertions pass; `docs-check` passes (rule 7 warning is non-blocking); no open exceptions in `docs/adr/`. | None. |

**Coverage score: 9 ✅ / 0 🟡 / 0 🔴** (out of 9 gates; up from 3/3/3 at first baseline; up from 7/0/2 after G5; up from 8/0/1 after G6).

---

## 5. Coverage matrix — v1.0 roadmap sections

The `v1.0-stabilization.md` roadmap adds five scope sections. Status:

### 5.1 Data safety

| Required work | Status | Evidence | Gap |
|---|:---:|---|---|
| format/migration support matrix | 🟡 | ADR-0045 declared; `migration.rs` exists; G2 runtime test proves round-trip is byte-identical | No declared matrix committed; no corpus large enough to be useful |
| backup/recovery | 🔴 | none | No backup path; OPFS is single-source-of-truth |
| interruption/crash tests for writes | 🔴 | none | No test for "kill mid-save" |
| import/reimport recovery | 🟡 | importer flow has ConflictPolicy per `e2e/importers.spec.ts` | No adversarial conflict corpus (rapid reimport during change) |

### 5.2 Performance

| Benchmark | Status | Evidence | Gap |
|---|:---:|---|---|
| 10k entities | 🔴 | 50 entities covered by `engine.spec.ts` | No 10k test |
| large tile levels | 🔴 | `auto-layer.spec.ts`, `tileset.spec.ts` exist | No size benchmark |
| multi-level worlds | 🟡 | `world-workspace.spec.ts` exists | Boundary cases not stress-tested |
| large asset catalogs | 🔴 | `asset-pipeline.spec.ts` exists | No large-catalog test |
| logic graphs | 🟡 | `logic-graph-persistence.spec.ts` exists | No complex-graph stress |
| project search/retrieval | 🟡 | `global-search-actions.spec.ts`, `ux-search.spec.ts` exist | No large-project search benchmark |

### 5.3 UX

| Required work | Status | Evidence | Gap |
|---|:---:|---|---|
| coherent onboarding/tutorial project | ✅ | `frontend/src/components/TutorialStepper.tsx` (5-card state machine, v0.110.6), `WelcomeOverlay`, `OnboardingBanner`, `ux-onboarding.spec.ts`, `ux-welcome.spec.ts`, `frontend/tests/tutorial-walkthrough.spec.ts` (4 scenarios × 2 projects = 8 tests); `examples/platformer-minimal/` is a guided sample | None |
| consistent shortcuts/menus/palette | ✅ | `keyboard-shortcuts.spec.ts`, `menu-visibility.spec.ts`, `ux-commands.spec.ts`, `ux-dock.spec.ts` | None |
| keyboard accessibility | ✅ | `keyboard-shortcuts.spec.ts`, `a11y-critical-paths.spec.ts` (5 tests: CP-1 welcome, CP-2 entity, CP-3 save, CP-4 play, CP-5 import), `docs/a11y-critical-paths.md` | None obvious. |
| clear mode/context breadcrumbs | ✅ | `mode-context-bar.spec.ts`, `mode-headers.spec.ts` | None |
| actionable validation messages | 🟡 | `validation-center-v2.spec.ts`, `validation-center-inbox.spec.ts` | No assertion on actionability (does each message link to a fix?) |

### 5.4 Developer ecosystem

| Required work | Status | Evidence | Gap |
|---|:---:|---|---|
| extension API compatibility policy | ✅ | `docs/compatibility-policy.md` (323 lines, 2026-09-07) + `crates/editor-model/tests/extension_compat.rs` (3 tests, v0.109.8) + ADR-0040/ADR-0043 | None |
| agent tool protocol versioning | 🟡 | ADR-0027/0043/0054 declared; capability enum + test coverage | Agent runtime deferred to last (user directive 2026-09-06); versioning not yet exercised |
| project format versioning | 🟡 | ADR-0045 declared; `project.json` has explicit `version` field; G2 runtime test verifies | No version registry; no upgrade tool |
| contribution architecture guide | 🔴 | `CONTRIBUTING.md` not present in repo root | Missing entirely |

### 5.5 Release engineering

| Required work | Status | Evidence | Gap |
|---|:---:|---|---|
| signed/versioned artifacts | 🔴 | Tags exist (`v0.109.8`) but no signing documented | No artifact signing in release pipeline |
| reproducible release pipeline | 🟡 | CI defined in `.github/workflows/` | No reproducibility test (two builds byte-equal?) |
| changelog/release notes from verified state | ✅ | `CHANGELOG.md` present; cycle artifacts include `archive-manifest.md` with verified sha256 | None |
| rollback/hotfix procedure | 🔴 | none | No documented procedure |

---

## 6. Gap analysis — priorities

### 6.1 Highest-impact gaps (block v1.0 declaration)

**No remaining gaps that block v1.0 declaration.** All nine v1.0 product gates are now `🟢`. Carry-forward items below remain amber/yellow, but the v1.0 product gates in §4 are all green.

### 6.2 Medium-impact gaps

3. Data-safety: backup/recovery path missing.
4. UX: no end-to-end tutorial project.
5. UX: comprehensive tab-order + focus-trap audit.
6. Release engineering: signed/versioned artifacts + rollback procedure.
7. Dev ecosystem: contribution architecture guide (`CONTRIBUTING.md`).

### 6.3 Pre-existing carry-forward (NOT introduced by this cycle)

- **C-3:** `cargo test --workspace --release --locked` exceeds 10 min on local runner; affects CI cycle time. This is a release-engineering concern; also blocks the "no regression" guarantee during the stabilization cycle.
- **M-2, M-3, M-5:** low-priority docs/archcheck warnings, not blockers.
- **rule-7 docs-check warnings:** 23 specs + 11 roadmaps without status markers; affects documentation hygiene.

### 6.4 Smoke cohort 60 s budget breach (pre-existing)

`playwright.smoke.config.ts` declares the cohort MUST stay under 60 s, but `engine.spec.ts` alone runs in ~1.2 m. Pre-existing condition; recovery-3 did not regress it. Resolution path: either trim `engine.spec.ts` from smoke cohort (move to `@full`) or split its cases into smaller specs.

---

## 7. Recommended next concrete work

All nine v1.0 product gates are green. Remaining work is **post-v1.0 candidate work**:
UX polish (tutorial, a11y trigger), release engineering (signed artefacts, rollback
procedure), perf budget tighten + raised MAX_SCENES, dev ecosystem (CONTRIBUTING.md).

Ordered by **impact × cost**:

### P0 — v1.0 declaration PR

- **Scope:** With all gates green, open a PR or release-notes entry declaring v1.0
  stabilization complete. The artefact is a `CHANGELOG.md` entry, a release tag,
  and a roadmap update.
- **Cost:** ~0.5 day.
- **Rationale:** the gate-matrix is in its terminal state; the user decides when
  to declare v1.0.

### P1 — Tutorial sample (UX onboarding) (closes UX §5.3 partial) — ✅ CLOSED in v0.110.6

- **Scope:** A guided tutorial that opens automatically when no project is detected. Walks user through: create entity → add component → create scene asset → save → reload → play mode.
- **Cost:** ~3 days.
- **Closes:** UX onboarding gap.
- **Resolution:** Closed in cycle `tutorial-walkthrough` (v0.110.6). 5-card guided walkthrough (Inspect Assets → Find Levels → Open Logic → Tweak Components → Play & Test) wired to WelcomeOverlay's "Take the tour" action. `TutorialStepper` mounts from `AppShell` via `tourOpen` state; steps 1+3 call `__setEditorMode`; step 1 calls `__loadSampleProject("platformer-minimal")` (stub — backend loader is a separate cycle). 8/8 tests pass; a11y intact (3/3 ux-welcome @accessibility regression). 4 files / +431/-1. Tag `v0.110.6` points at commit `c23d84e`. Carry-forward (separate cycles): wire `__loadSampleProject` to backend OPFS loader; persist "completed tour" so the welcome card greys out; fix `ux-welcome.spec.ts` @full cohort (pre-existing at base 72d9c4a).

### P2 — CP-5 import trigger (closes a11y gap) — ✅ CLOSED in v0.110.4

- **Scope:** Add a keyboard-accessible Import Asset trigger (button or menu item) with a stable `data-testid`. Then add a Playwright test asserting the contract.
- **Artifact:** `frontend/src/components/MenuBar.tsx` (add menu item) or `frontend/src/components/AssetNavigator.tsx` (add button); update `docs/a11y-critical-paths.md` CP-5 row.
- **Cost:** ~0.5 day.
- **Closes:** CP-5 deferred.
- **Resolution:** Closed in cycle `cp5-import-trigger` (v0.110.4). Adds `import-asset-btn` to `frontend/src/components/ProjectAssetBrowser.tsx` (sibling to `import-bsn-btn`) with `aria-label`, hidden file picker filtered to `.aseprite,.ase,.ldtk,.tmx,.json,.png`. Test `cp5_import_asset_button_has_aria_label_and_is_keyboard_focusable` added. CP-3/CP-4 made `inert`-tolerant. CP-5 carry-forward: `<ImportDialog />` wiring is a separate cycle.

### P3 — Perf budget tightening (post-v1.0 maintenance)

- **Scope:** The G6 corpus already emits soft-budget warnings on P2 (paint/erase)
  and P5 (logic dispatch). Tighten the soft budgets by 1.5× once the next round
  of perf optimisations lands. Add `nightly-perf.yml` CI workflow to track the
  corpus on a daily schedule.
- **Cost:** ~1 day.
- **Rationale:** keeps the corpus useful as a regression detector.

### P4 — Trim smoke cohort (housekeeping)

- **Scope:** Move `engine.spec.ts` out of `playwright.smoke.config.ts`. Keep only `smoke.spec.ts`, `app-characterization.spec.ts`, `capabilities-smoke.spec.ts`, `editor-ready.spec.ts` in smoke. Add `engine.spec.ts` to `playwright.full.config.ts` only.
- **Cost:** ~0.5 day.
- **Closes:** pre-existing 60 s budget breach.

---

## 8. What this document does NOT cover

- **Rig agent runtime:** explicitly deferred per user directive 2026-09-06.
- **Post-v1 items:** collaborative editing, plugin system (already deferred to post-Hito 8 in MASTER_ROADMAP).
- **CI optimization (C-3):** tracked separately in the cycle's carry-forward list.

---

## 9. Decision points for next session

1. **Start with P1 (crash recovery)?** High-leverage gate-closer; G5 is the largest remaining blocker.
2. **Or P2 (performance corpus)?** Largest content (6 specs) but the canonical sample exists now (G1 ✅), so the corpus can be built against real content.
3. **Or P3 (v1 format list)?** Small doc-only commit; locks the format contract before new work adds migration paths.

Recommend: **P1 first** (closes a 🔴), then **P3** (locks format contract), then **P4** (closes a 🟡).

---

## 10. Carry-forward from this refresh

- **Inventory stale-read:** test counts and feature surface drift between this snapshot and HEAD `0c658e6`. Recommend running a refresh once per release cycle.
- **Coverage score freeze:** the score `9 ✅ / 0 🟡 / 0 🔴` is the **terminal v1.0 state**. Future cycles operate on 🟡 items in `v1.0-stabilization.md` §5 (UX, perf tighten, release eng, dev ecosystem), not on 🟢/🔴 gate-matrix items.
- **Cross-references:** cycle artifacts in `docs/sddk/<cycle>/archive-manifest.md` are the authoritative evidence ledger; this map only summarises the current state.
- **This map is the gate-matrix definition of done for v1.0.** When this document reads 9 ✅ / 0 / 0 the v1.0 product gates are complete.
