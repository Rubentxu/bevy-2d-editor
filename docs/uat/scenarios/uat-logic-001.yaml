schema: bevy-editor-uat/v1
id: UAT-LOGIC-001
version: 1
title: "Create and execute Platformer Jump logic"
persona: technical-designer
priority: P1
release_gate: v1
modes:
  - manual-guided
  - playwright
  - hybrid
goal: "Create Sensor -> Controller -> Actuator workflow and inspect activation."
fixture:
  project: fixtures/logic_001
preconditions:
  - "Use an isolated copy of the fixture project."
steps:
  - id: step-1
    instruction: "Open platformer fixture."
    action:
      kind: manual-or-adapter
      value: "Open platformer fixture."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-2
    instruction: "Select Player."
    action:
      kind: manual-or-adapter
      value: "Select Player."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-3
    instruction: "Add/create Platformer Jump logic."
    action:
      kind: manual-or-adapter
      value: "Add/create Platformer Jump logic."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-4
    instruction: "Enter Play."
    action:
      kind: manual-or-adapter
      value: "Enter Play."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-5
    instruction: "Trigger jump input and open Trace."
    action:
      kind: manual-or-adapter
      value: "Trigger jump input and open Trace."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
final:
  runtime:
    - "activation path contains sensor -> controller -> actuator"
    - "Player runtime state changes"
  causality:
    - "activation linked to graph/revision"
evidence:
  level: E3
  capture:
    - screenshot-on-failure
    - changesets
    - validation
    - semantic-summary
