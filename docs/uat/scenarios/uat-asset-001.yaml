schema: bevy-editor-uat/v1
id: UAT-ASSET-001
version: 1
title: "Rename referenced asset safely"
persona: content-designer
priority: P1
release_gate: v1
modes:
  - playwright
  - hybrid
goal: "Rename an asset and preserve references."
fixture:
  project: fixtures/asset_001
preconditions:
  - "Use an isolated copy of the fixture project."
steps:
  - id: step-1
    instruction: "Open dependency fixture."
    action:
      kind: manual-or-adapter
      value: "Open dependency fixture."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-2
    instruction: "Rename a referenced asset."
    action:
      kind: manual-or-adapter
      value: "Rename a referenced asset."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-3
    instruction: "Review ChangeSet effects."
    action:
      kind: manual-or-adapter
      value: "Review ChangeSet effects."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-4
    instruction: "Save and reload."
    action:
      kind: manual-or-adapter
      value: "Save and reload."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
  - id: step-5
    instruction: "Open affected Scene Assets."
    action:
      kind: manual-or-adapter
      value: "Open affected Scene Assets."
    expect:
      checkpoint: "No unexpected critical error; validate relevant final assertions."
final:
  semantic:
    - "references resolve"
    - "no validation errors"
  graph:
    - "old reference absent"
    - "new reference dependencies present"
evidence:
  level: E2
  capture:
    - screenshot-on-failure
    - changesets
    - validation
    - semantic-summary
