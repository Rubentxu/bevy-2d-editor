# Tasks: h2-5-runtime-coordination (high-level decomposition)

> Generated by orchestrator as recovery action (sddk-tasks phase spawn was
> stuck). `sddk-tasks` agent should refine into review-aware work units.

## Decomposition (3 blocks)

### Block A — Runtime Buses → EditorSession.runtime (Group A)

**File scope:**
- `crates/editor-model/src/runtime/` (NEW: `linear_bus.rs`, `actuator_bus.rs`, `hot_reload.rs`)
- `crates/editor-model/src/lib.rs` (re-exports)
- `crates/editor-application/src/session.rs` (`RuntimeSessionState` gains 5 fields + accessors)
- `crates/editor-model/src/session_port.rs` (5 new methods on `EditorSessionPort`)
- `crates/editor-bevy/src/lib.rs` (remove `COMMAND_BUS`/`EVENT_BUS` thread_locals; trampolines)
- `crates/editor-bevy/src/hot_reload_state.rs` (remove thread_locals; trampolines)
- `crates/editor-bevy/src/actuator_bus.rs` (remove thread_local; trampoline)
- `crates/editor-bevy/tests/hot_reload.rs` (parity — should pass unchanged)
- `crates/editor-bevy/tests/play_mode.rs` (parity)
- `crates/editor-bevy/tests/apply_actuator_outputs_in_preview.rs` (parity)

**Steps:**
1. Move `LinearBus`, `ActuatorBus`, `HotReloadRequest`, `PlayModeRequest` to
   `crates/editor-model/src/runtime/` with `#[derive(Debug, Default, Clone)]`.
2. Add 5 fields + accessors to `RuntimeSessionState`; eager init in
   `EditorSession::new()`.
3. Extend `EditorSessionPort` with 5 new `*_mut` methods; impl on
   `EditorSession` routes to `self.runtime.<field>_mut()`.
4. Replace thread_local `with()` / `borrow_mut()` in `lib.rs`,
   `hot_reload_state.rs`, `actuator_bus.rs` with `with_session_mut`
   closures (trampoline signatures unchanged).
5. Run `cargo fmt` + `cargo check --workspace` + parity tests.

### Block B — Preview Inspector → EditorSession.preview_state (Group B)

**File scope:**
- `crates/editor-model/src/session.rs` (`PreviewInspectorState` gains typed backing)
- `crates/editor-bevy/src/preview_inspector.rs` (typed trampolines; new
  `project_preview_inspector_json` Bevy system)
- `crates/editor-bevy/src/state.rs` (register new system if a `Plugin` lists
  systems here — check during apply)
- `crates/editor-bevy/tests/rebuild_cause_unified.rs` (migrate fixture
  from `PREVIEW_PROVENANCE.with(...)` to `with_session_mut`)
- `crates/editor-bevy/tests/preview_state_parity.rs` (NEW: golden test
  for typed ↔ JSON projection)

**Steps:**
1. Add 3 typed backing fields to `PreviewInspectorState`.
2. Confirm `PreviewMetrics` / `PreviewMappingEntry` / `PreviewProvenance`
   already `Serialize` + `Clone` (they are; verify in editor-model).
3. Add a `dirty: bool` field (private) to `PreviewInspectorState` (or a
   separate `PreviewProjectionDirty` flag) so the projection system
   short-circuits.
4. Trampolines write typed fields + set dirty flag.
5. New Bevy system `project_preview_inspector_json` in `Update` schedule,
   early — reads dirty flag, regenerates JSON, clears flag.
6. Migrate `rebuild_cause_unified.rs` fixture.
7. Add `preview_state_parity.rs` golden test (typed → JSON round-trip).
8. Run parity suite: `cargo test -p editor-bevy hot_reload play_mode
   rebuild_cause_unified apply_actuator_outputs_in_preview
   preview_state_parity`.

### Block C — KEYBOARD_STATE → Bevy Resource InputState (Group C)

**File scope:**
- `crates/editor-bevy/src/input_state.rs` (NEW)
- `crates/editor-bevy/src/lib.rs` (register `InputStatePlugin`; remove
  `KEYBOARD_STATE` thread_local)
- `crates/editor-bevy/src/logic_evaluator.rs` (`update_keyboard_state`
  signature changes; sensor node reads `Res<InputState>`)
- `crates/editor-bevy/tests/keyboard_input_state.rs` (NEW: boot order
  test for UAT-ARCH-003)

**Steps:**
1. Create `InputState { held_keys: HashSet<String> }` Resource + `Default`.
2. Create `InputStatePlugin` that `init_resource::<InputState>()` and
   `add_systems(Update, update_keyboard_state)`.
3. Update `update_keyboard_state` to read `Res<ButtonInput<KeyCode>>`
   and write `ResMut<InputState>::held_keys`.
4. Replace `KEYBOARD_STATE.with(...)` reads in sensor nodes with
   `Res<InputState>::held_keys`.
5. Register `InputStatePlugin` in the Bevy app initialization.
6. Remove `thread_local! KEYBOARD_STATE` from `logic_evaluator.rs`.
7. Add boot order test (`keyboard_input_state.rs`): instantiate Bevy App
   with `InputStatePlugin`, verify `Res<InputState>` is valid in the
   first frame.
8. Run `cargo test -p editor-bevy keyboard_input_state`.

### Block D — Inventory + Matrix + Final verification

1. `tools/archcheck-globals/globals-inventory.yaml`: remove 9 entries.
2. `docs/architecture/state-ownership-matrix.md` § H2.5: mark rows
   RETIRED with PR reference.
3. `cargo fmt --all --check`, `cargo check --workspace --all-targets`,
   `cargo test --workspace`.
4. Verify UAT-ARCH-003 (no hidden init order) via
   `keyboard_input_state.rs` boot order test + a fresh-app test.
5. Verify UAT-ARCH-004 (independent sessions) via a new test that
   constructs two `EditorSession` instances and asserts their runtime
   state is independent.

## Order

Recommended order: A → B → C → D. A and B can be combined into a single
PR if the diff stays < 1500 lines (precedent: H2.4 was ~700 lines).
C is small and isolated. D wraps everything.

## Cross-cutting concerns

- Each block's parity tests are NON-NEGOTIABLE.
- `archcheck-globals` must report zero matches for the 9 retired cells
  after Block D.
- Two independent `EditorSession` instances must be constructible in
  the same process with no cross-session leakage (UAT-ARCH-004).